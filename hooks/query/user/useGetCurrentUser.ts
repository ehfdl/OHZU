// import { getCurrentUser } from "@/api/userAPI";
// import { useQuery } from "@tanstack/react-query";

// export const useGetCurrentUser = (id: string) => {
//   return useQuery(["currentUser", id], () => getCurrentUser(id));
// };
